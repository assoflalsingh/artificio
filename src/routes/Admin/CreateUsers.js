import React from 'react';
import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({
}));

export default function CreateUsers({match}) {
  const classes = useStyles();

  return (
    <h1>CreateUsers</h1>
  );
}